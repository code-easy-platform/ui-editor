import { TElement } from '../../../types';


interface IEditProps {
  element: TElement;
  parents: TElement[];
}
export const Edit = ({ element, parents }: IEditProps) => {

  return (
    <>
      Edit html
    </>
  );
};
