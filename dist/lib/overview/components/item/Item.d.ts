interface IItemProps {
    label: string;
    hover: boolean;
    select: boolean;
    paddingLeft: number;
}
export declare const Item: ({ label, hover, select, paddingLeft }: IItemProps) => import("react/jsx-runtime").JSX.Element;
export {};
